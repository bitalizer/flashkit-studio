"""flashkit_studio — Qt (PySide6) front-end for the flashkit SWF
inspector.

A retained-mode desktop app. Dark flat palette with a single cyan
accent, monospace code with per-token syntax colours and native
text selection.

Run with ``python -m flashkit_studio``.
"""

__version__ = "0.1.0"
